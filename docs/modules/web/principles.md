# Web 模块原理文档（Phase 3）

## 1. 简单前端原则
- 前端仅使用原生 HTML + JavaScript，减少构建链复杂度，便于作业验收与演示。

## 2. 单页实时刷新
- 页面初始化拉取一次结果，用户每次投票后直接使用响应刷新列表，保证“所见即所得”。

## 3. 同源调用
- 静态资源由 HttpServer 托管，接口也由同一服务暴露，天然同源，避免跨域问题。

## 4. 错误可见性
- 前端会展示后端返回错误，帮助快速定位非法话题或后端不可用等问题。
