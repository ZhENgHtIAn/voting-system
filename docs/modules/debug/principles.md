# Debug 模块原理文档

## 1. 就地可调试
- 调试客户端与服务端代码同仓维护，减少外部工具依赖与环境差异。

## 2. 最小协议闭环
- 调试入口仅覆盖 `GetResults` 与 `CastVote`，与 Phase 2 契约完全一致，便于快速回归核心链路。

## 3. 可读输出优先
- 结果按话题名排序输出，便于连续多次投票时做人工对比。

## 4. 低侵入
- 调试模块不改变业务服务逻辑，不影响 GrpcServer 运行时行为。
