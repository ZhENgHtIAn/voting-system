# Testing 模块原理文档（Phase 4）

## 1. 接口隔离
- 通过 `VoteClient` 接口注入 mock，实现不依赖真实 gRPC/Redis 的单元测试。

## 2. 行为优先
- 测试围绕 API 外部可观察行为（HTTP 状态码、响应体）而非内部实现细节。

## 3. 错误映射保障
- 显式覆盖 gRPC 错误到 HTTP 错误的映射规则，避免网关行为漂移。

## 4. 渐进覆盖
- 先覆盖核心 Handler 路径，再逐步扩展到更多错误与超时分支，以提升整体覆盖率。
